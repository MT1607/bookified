event: message
data: {"result":{"content":[{"type":"text","text":"# B2B-SAAS Bundle\n\nComplete B2B SaaS setup with organizations, billing, and role-based access\n\nIncludes: use-user, use-auth, use-organization, use-organization-list, b2b-saas-setup, billing-integration, organization-switcher, show-component, server-auth-nextjs, clerk-client-backend\n\n---\n\n\n# useUser hook\n\nReturns the current user object and loading/signed-in state. Available in React, Next.js, Expo, React Router, Chrome Extension, and TanStack React Start.\n\n```typescript\nimport { useUser } from '@clerk/clerk-react';\n// Or: '@clerk/nextjs', '@clerk/clerk-expo', '@clerk/react-router', etc.\n\nexport function UserProfile() {\n  const { isLoaded, isSignedIn, user } = useUser();\n\n  if (!isLoaded) {\n    return <div>Loading...</div>;\n  }\n\n  if (!isSignedIn) {\n    return <div>Not signed in</div>;\n  }\n\n  return (\n    <div>\n      <h1>Welcome, {user.firstName}!</h1>\n      <p>Email: {user.primaryEmailAddress?.emailAddress}</p>\n      <img src={user.imageUrl} alt=\"Profile\" />\n    </div>\n  );\n}\n```\n\n## Return Type\n\n- `isLoaded: boolean` - Whether user data has loaded\n- `isSignedIn: boolean | undefined` - Whether user is signed in (undefined while loading)\n- `user: User | null | undefined` - The User object when signed in\n\n## Common User Properties\n\n- `user.id` - Unique user ID\n- `user.firstName`, `user.lastName`, `user.fullName`\n- `user.primaryEmailAddress?.emailAddress`\n- `user.imageUrl` - Profile image URL\n- `user.publicMetadata` - Public metadata (visible client-side)\n- `user.unsafeMetadata` - User-editable metadata\n\n## User Methods\n\n- `user.update(params)` - Update user profile data (name, username, etc.)\n- `user.reload()` - Refresh user data from server (useful after server-side updates)\n\n\n---\n\n\n# useAuth hook\n\nReturns auth state and methods for getting tokens. Essential for making authenticated API calls.\n\n```typescript\nimport { useAuth } from '@clerk/clerk-react';\n// Or: '@clerk/nextjs', '@clerk/clerk-expo', '@clerk/astro/react', etc.\n\nexport function AuthenticatedApiCall() {\n  const { isLoaded, isSignedIn, userId, sessionId, getToken } = useAuth();\n\n  const fetchData = async () => {\n    // Get token for your own backend\n    const token = await getToken();\n\n    // Or get token for a specific integration (e.g., Supabase, Firebase)\n    const supabaseToken = await getToken({ template: 'supabase' });\n\n    const response = await fetch('/api/protected', {\n      headers: {\n        Authorization: `Bearer ${token}`,\n      },\n    });\n    return response.json();\n  };\n\n  if (!isLoaded) return <div>Loading...</div>;\n  if (!isSignedIn) return <div>Sign in to continue</div>;\n\n  return <button onClick={fetchData}>Fetch Protected Data</button>;\n}\n```\n\n## Return Type\n\n- `isLoaded: boolean` - Whether auth state has loaded\n- `isSignedIn: boolean | undefined` - Whether user is signed in\n- `userId: string | null | undefined` - Current user's ID\n- `sessionId: string | null | undefined` - Current session ID\n- `orgId: string | null | undefined` - Active organization ID (if any)\n- `orgRole: string | null | undefined` - User's role in active org\n- `orgSlug: string | null | undefined` - Active organization's slug\n- `getToken(options?)` - Get a session token for API calls\n- `signOut()` - Sign the user out\n\n## Token Templates\n\nUse `getToken({ template: 'name' })` to get JWTs customized for integrations:\n\n- `supabase` - For Supabase integration\n- `firebase` - For Firebase integration\n- `convex` - For Convex integration\n- Custom templates configured in Clerk Dashboard\n\n\n---\n\n\n# useOrganization hook\n\nAccess the currently active organization and manage its members, invitations, and settings.\n\n```typescript\nimport { useOrganization } from '@clerk/clerk-react';\n// Or: '@clerk/nextjs', '@clerk/clerk-expo', '@clerk/remix', etc.\n\nexport function OrganizationDashboard() {\n  const {\n    isLoaded,\n    organization,\n    membership,\n    memberships,\n    invitations,\n  } = useOrganization({\n    memberships: { infinite: true },\n    invitations: { infinite: true },\n  });\n\n  if (!isLoaded) return <div>Loading...</div>;\n  if (!organization) return <div>No organization selected</div>;\n\n  return (\n    <div>\n      <h1>{organization.name}</h1>\n      <img src={organization.imageUrl} alt={organization.name} />\n      <p>Your role: {membership?.role}</p>\n      <p>Members: {organization.membersCount}</p>\n\n      <h2>Team Members</h2>\n      <ul>\n        {memberships?.data?.map((member) => (\n          <li key={member.id}>\n            {member.publicUserData.firstName} - {member.role}\n          </li>\n        ))}\n      </ul>\n\n      <h2>Pending Invitations</h2>\n      <ul>\n        {invitations?.data?.map((inv) => (\n          <li key={inv.id}>\n            {inv.emailAddress} - {inv.status}\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n}\n```\n\n## Inviting Members\n\n```typescript\nconst { organization } = useOrganization();\n\nconst inviteUser = async (email: string, role: 'admin' | 'member') => {\n  await organization?.inviteMember({\n    emailAddress: email,\n    role,\n  });\n};\n```\n\n## Updating Organization\n\n```typescript\nconst { organization } = useOrganization();\n\n// Update name\nawait organization?.update({ name: 'New Company Name' });\n\n// Update logo\nawait organization?.setLogo({ file: imageFile });\n```\n\n## Return Type\n\n- `organization` - The active Organization object\n- `membership` - Current user's membership in this org\n- `memberships` - Paginated list of all members\n- `invitations` - Paginated list of pending invitations\n- `membershipRequests` - Paginated list of membership requests\n\n\n---\n\n\n# useOrganizationList hook\n\nList all organizations a user belongs to and switch between them.\n\n```typescript\nimport { useOrganizationList } from '@clerk/clerk-react';\n// Or: '@clerk/nextjs', '@clerk/clerk-expo', '@clerk/remix', etc.\n\nexport function OrganizationSwitcher() {\n  const {\n    isLoaded,\n    setActive,\n    userMemberships,\n    userInvitations,\n    createOrganization,\n  } = useOrganizationList({\n    userMemberships: { infinite: true },\n    userInvitations: { infinite: true },\n  });\n\n  if (!isLoaded) return <div>Loading...</div>;\n\n  const switchOrg = async (orgId: string) => {\n    await setActive({ organization: orgId });\n  };\n\n  const handleCreateOrg = async () => {\n    const org = await createOrganization({ name: 'New Organization' });\n    await setActive({ organization: org.id });\n  };\n\n  return (\n    <div>\n      <h2>Your Organizations</h2>\n      <ul>\n        {userMemberships?.data?.map((mem) => (\n          <li key={mem.id}>\n            <button onClick={() => switchOrg(mem.organization.id)}>\n              {mem.organization.name} ({mem.role})\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <h2>Pending Invitations</h2>\n      <ul>\n        {userInvitations?.data?.map((inv) => (\n          <li key={inv.id}>\n            {inv.publicOrganizationData.name}\n            <button onClick={() => inv.accept()}>Accept</button>\n            <button onClick={() => inv.reject()}>Decline</button>\n          </li>\n        ))}\n      </ul>\n\n      <button onClick={handleCreateOrg}>Create Organization</button>\n    </div>\n  );\n}\n```\n\n## Switch to Personal Account\n\n```typescript\nconst { setActive } = useOrganizationList();\n\n// Set organization to null for personal account\nawait setActive({ organization: null });\n```\n\n## Return Type\n\n- `userMemberships` - Orgs the user is a member of\n- `userInvitations` - Pending invitations for the user\n- `userSuggestions` - Suggested organizations\n- `setActive()` - Switch active organization\n- `createOrganization()` - Create a new organization\n\n\n---\n\n\n# B2B SaaS Setup with Clerk\n\nComplete setup for a B2B SaaS application with organizations, roles, and billing.\n\n**Requirements**: Next.js 13+ with App Router\n\n## 1. Enable Organizations\n\nIn Clerk Dashboard:\n\n1. Go to **Organizations** → **Settings**\n2. Enable **Organizations**\n3. Configure allowed roles (e.g., admin, member, billing)\n\n## 2. App Layout with Organization Support\n\n```typescript\n// app/layout.tsx\nimport {\n  ClerkProvider,\n  SignedIn,\n  SignedOut,\n  UserButton,\n  OrganizationSwitcher,\n} from '@clerk/nextjs';\n\nexport default function RootLayout({ children }) {\n  return (\n    <ClerkProvider>\n      <html>\n        <body>\n          <header>\n            <SignedOut>\n              <a href=\"/sign-in\">Sign In</a>\n            </SignedOut>\n            <SignedIn>\n              <OrganizationSwitcher\n                hidePersonal={true} // Force org selection for B2B\n                afterSelectOrganizationUrl=\"/dashboard\"\n                afterCreateOrganizationUrl=\"/onboarding\"\n              />\n              <UserButton />\n            </SignedIn>\n          </header>\n          {children}\n        </body>\n      </html>\n    </ClerkProvider>\n  );\n}\n```\n\n## 3. Protect Routes by Organization\n\n```typescript\n// middleware.ts\nimport { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server';\n\nconst isProtectedRoute = createRouteMatcher(['/dashboard(.*)']);\nconst isAdminRoute = createRouteMatcher(['/dashboard/settings(.*)']);\n\nexport default clerkMiddleware(async (auth, req) => {\n  const { orgId, orgRole } = await auth();\n\n  if (isProtectedRoute(req)) {\n    await auth.protect();\n\n    // Require organization for dashboard\n    if (!orgId) {\n      return Response.redirect(new URL('/select-org', req.url));\n    }\n  }\n\n  if (isAdminRoute(req)) {\n    // Only admins can access settings\n    if (orgRole !== 'org:admin') {\n      return Response.redirect(new URL('/dashboard', req.url));\n    }\n  }\n});\n```\n\n## 4. Role-Based UI\n\n```typescript\nimport { useOrganization, Protect } from '@clerk/nextjs';\n\nexport function DashboardSidebar() {\n  const { membership } = useOrganization();\n  const isAdmin = membership?.role === 'org:admin';\n\n  return (\n    <nav>\n      <a href=\"/dashboard\">Dashboard</a>\n      <a href=\"/dashboard/team\">Team</a>\n\n      {/* Only show to admins */}\n      <Protect role=\"org:admin\">\n        <a href=\"/dashboard/settings\">Settings</a>\n        <a href=\"/dashboard/billing\">Billing</a>\n      </Protect>\n    </nav>\n  );\n}\n```\n\n## 5. Server-Side Organization Check\n\n```typescript\n// app/api/team/route.ts\nimport { auth } from '@clerk/nextjs/server';\n\nexport async function GET() {\n  const { userId, orgId, orgRole } = await auth();\n\n  if (!userId || !orgId) {\n    return Response.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n\n  // Fetch team data scoped to organization\n  const team = await db.teamMembers.findMany({\n    where: { organizationId: orgId },\n  });\n\n  return Response.json(team);\n}\n```\n\n\n---\n\n\n# Stripe Billing Integration\n\nIntegrate Stripe subscriptions with Clerk for B2B billing.\n\n**Important**: All Stripe instances use API version `2025-12-15.clover`. Update this version if you encounter TypeScript errors about API version mismatches. Check [Stripe's API versioning docs](https://docs.stripe.com/api/versioning) for the latest version.\n\n## 1. Store Stripe Customer ID\n\n```typescript\n// When organization is created, create Stripe customer\nimport { clerkClient } from '@clerk/nextjs/server';\nimport Stripe from 'stripe';\n\nconst stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {\n  apiVersion: '2025-12-15.clover',\n});\n\nexport async function createStripeCustomer(orgId: string, orgName: string, email: string) {\n  const client = await clerkClient();\n\n  const customer = await stripe.customers.create({\n    name: orgName,\n    email,\n    metadata: { clerkOrgId: orgId },\n  });\n\n  // Store Stripe customer ID in org metadata\n  await client.organizations.updateOrganization(orgId, {\n    publicMetadata: {\n      stripeCustomerId: customer.id,\n    },\n  });\n\n  return customer;\n}\n```\n\n## 2. Billing Page Component\n\n```typescript\n'use client';\nimport { useOrganization } from '@clerk/clerk-react';\nimport { useState } from 'react';\n\nexport function BillingPage() {\n  const { organization, membership } = useOrganization();\n  const [loading, setLoading] = useState(false);\n\n  const isAdmin = membership?.role === 'org:admin';\n  const plan = (organization?.publicMetadata?.plan as string) || 'free';\n\n  const handleUpgrade = async (priceId: string) => {\n    setLoading(true);\n    const response = await fetch('/api/billing/create-checkout', {\n      method: 'POST',\n      body: JSON.stringify({ priceId }),\n    });\n    const { url } = await response.json();\n    window.location.href = url;\n  };\n\n  const handleManage = async () => {\n    const response = await fetch('/api/billing/create-portal', {\n      method: 'POST',\n    });\n    const { url } = await response.json();\n    window.location.href = url;\n  };\n\n  if (!isAdmin) {\n    return <p>Only admins can manage billing.</p>;\n  }\n\n  return (\n    <div>\n      <h1>Billing</h1>\n      <p>Current plan: {plan}</p>\n\n      {plan === 'free' ? (\n        <div>\n          <button onClick={() => handleUpgrade('price_pro_monthly')}>\n            Upgrade to Pro\n          </button>\n          <button onClick={() => handleUpgrade('price_enterprise_monthly')}>\n            Upgrade to Enterprise\n          </button>\n        </div>\n      ) : (\n        <button onClick={handleManage}>Manage Subscription</button>\n      )}\n    </div>\n  );\n}\n```\n\n## 3. Checkout API Route\n\n```typescript\n// app/api/billing/create-checkout/route.ts\nimport { auth, clerkClient } from '@clerk/nextjs/server';\nimport Stripe from 'stripe';\n\nconst stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {\n  apiVersion: '2025-12-15.clover',\n});\n\nexport async function POST(req: Request) {\n  const { userId, orgId } = await auth();\n  if (!userId || !orgId) {\n    return Response.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n\n  const { priceId } = await req.json();\n  const client = await clerkClient();\n  const org = await client.organizations.getOrganization({\n    organizationId: orgId,\n  });\n\n  let customerId = org.publicMetadata?.stripeCustomerId as string;\n\n  // Create customer if doesn't exist\n  if (!customerId) {\n    const customer = await stripe.customers.create({\n      metadata: { clerkOrgId: orgId },\n    });\n    customerId = customer.id;\n    await client.organizations.updateOrganization(orgId, {\n      publicMetadata: { stripeCustomerId: customerId },\n    });\n  }\n\n  const session = await stripe.checkout.sessions.create({\n    customer: customerId,\n    mode: 'subscription',\n    line_items: [{ price: priceId, quantity: 1 }],\n    success_url: `${process.env.NEXT_PUBLIC_URL}/dashboard/billing?success=true`,\n    cancel_url: `${process.env.NEXT_PUBLIC_URL}/dashboard/billing?canceled=true`,\n    metadata: { clerkOrgId: orgId },\n  });\n\n  return Response.json({ url: session.url });\n}\n```\n\n## 4. Webhook Handler\n\n```typescript\n// app/api/webhooks/stripe/route.ts\nimport { clerkClient } from '@clerk/nextjs/server';\nimport Stripe from 'stripe';\n\nconst stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {\n  apiVersion: '2025-12-15.clover',\n});\n\nexport async function POST(req: Request) {\n  const body = await req.text();\n  const sig = req.headers.get('stripe-signature')!;\n  const client = await clerkClient();\n\n  const event = stripe.webhooks.constructEvent(body, sig, process.env.STRIPE_WEBHOOK_SECRET!);\n\n  if (event.type === 'checkout.session.completed') {\n    const session = event.data.object as Stripe.Checkout.Session;\n    const orgId = session.metadata?.clerkOrgId;\n\n    if (orgId) {\n      await client.organizations.updateOrganization(orgId, {\n        publicMetadata: {\n          plan: 'pro',\n          stripeSubscriptionId: session.subscription,\n        },\n      });\n    }\n  }\n\n  if (event.type === 'customer.subscription.deleted') {\n    const subscription = event.data.object as Stripe.Subscription;\n    const customer = await stripe.customers.retrieve(subscription.customer as string);\n    const orgId = (customer as Stripe.Customer).metadata?.clerkOrgId;\n\n    if (orgId) {\n      await client.organizations.updateOrganization(orgId, {\n        publicMetadata: { plan: 'free', stripeSubscriptionId: null },\n      });\n    }\n  }\n\n  return Response.json({ received: true });\n}\n```\n\n\n---\n\n\n# OrganizationSwitcher Component\n\nPre-built dropdown for switching between organizations.\n\n```typescript\nimport { OrganizationSwitcher } from '@clerk/clerk-react';\n// Or: '@clerk/nextjs', '@clerk/clerk-expo', '@clerk/remix', etc.\n\nexport function Header() {\n  return (\n    <header>\n      <OrganizationSwitcher\n        // Hide personal workspace (for B2B apps)\n        hidePersonal={true}\n        // Redirect after org selection\n        afterSelectOrganizationUrl=\"/dashboard\"\n        afterCreateOrganizationUrl=\"/onboarding\"\n        afterLeaveOrganizationUrl=\"/select-org\"\n        // Show create organization option\n        createOrganizationMode=\"modal\" // or \"navigation\"\n        createOrganizationUrl=\"/create-org\"\n        // Customize appearance\n        appearance={{\n          elements: {\n            rootBox: 'my-custom-class',\n            organizationSwitcherTrigger: 'border rounded-lg px-4 py-2',\n          },\n        }}\n      />\n    </header>\n  );\n}\n```\n\n## Force Organization Selection\n\nFor B2B apps, ensure users always have an org selected:\n\n```typescript\n// middleware.ts\nimport { clerkMiddleware } from '@clerk/nextjs/server';\n\nexport default clerkMiddleware(async (auth, req) => {\n  const { orgId } = await auth();\n\n  // Allow org selection page\n  if (req.nextUrl.pathname === '/select-org') {\n    return;\n  }\n\n  // Redirect to org selection if none active\n  if (req.nextUrl.pathname.startsWith('/dashboard') && !orgId) {\n    return Response.redirect(new URL('/select-org', req.url));\n  }\n});\n```\n\n\n---\n\n\n# Show Component\n\nConditionally render content based on user authentication and authorization (roles, permissions, features, plans).\n\n## Minimum SDK Versions\n\nThe `<Show>` component requires the following minimum SDK versions:\n\n- `@clerk/react` >= 6.0.0\n- `@clerk/nextjs` >= 7.0.0\n- `@clerk/expo` >= 3.0.0\n- `@clerk/astro` >= 3.0.0\n- `@clerk/react-router` >= 3.0.0\n- `@clerk/chrome-extension` >= 3.0.0\n- `@clerk/tanstack-react-start` >= 1.0.0\n- `@clerk/vue` >= 2.0.0\n- `@clerk/nuxt` >= 2.0.0\n\n**Security Note**: This component only visually hides content. The HTML remains accessible in the browser source. For truly sensitive data, perform authorization checks on the server.\n\n```typescript\nimport { Show } from '@clerk/clerk-react';\n// Or: '@clerk/nextjs', '@clerk/clerk-expo', '@clerk/astro', etc.\n\nexport function Dashboard() {\n  return (\n    <div>\n      {/* Only visible to authenticated users */}\n      <Show when=\"signed-in\">\n        <p>Welcome back!</p>\n      </Show>\n\n      {/* Only visible to signed-out users */}\n      <Show when=\"signed-out\">\n        <p>Please sign in to continue.</p>\n      </Show>\n\n      {/* Only visible to org admins */}\n      <Show when={{ role: 'org:admin' }}>\n        <button>Delete Organization</button>\n      </Show>\n\n      {/* Only visible to users with specific permission */}\n      <Show when={{ permission: 'org:billing:manage' }}>\n        <a href=\"/billing\">Manage Billing</a>\n      </Show>\n\n      {/* Check for feature access */}\n      <Show when={{ feature: 'premium_access' }}>\n        <PremiumFeatures />\n      </Show>\n\n      {/* Check for plan subscription */}\n      <Show when={{ plan: 'pro' }}>\n        <ProFeatures />\n      </Show>\n\n      {/* Fallback for unauthorized users */}\n      <Show\n        when={{ role: 'org:admin' }}\n        fallback={<p>Only admins can see this section.</p>}\n      >\n        <AdminSettings />\n      </Show>\n\n      {/* Condition-based with custom logic */}\n      <Show\n        when={(has) =>\n          has({ role: 'org:admin' }) || has({ permission: 'org:billing:manage' })\n        }\n      >\n        <BillingSection />\n      </Show>\n    </div>\n  );\n}\n```\n\n## Props\n\n- `when` - **Required.** Determines when to show content:\n  - `\"signed-in\"` - Show when user is authenticated\n  - `\"signed-out\"` - Show when user is not authenticated\n  - `{ role: string }` - Check for organization role (format: `org:role_name`)\n  - `{ permission: string }` - Check for permission (format: `org:resource:action`)\n  - `{ feature: string }` - Check for feature access\n  - `{ plan: string }` - Check for plan subscription\n  - `(has) => boolean` - Custom authorization logic using `has()` function\n- `fallback?` - Component to render if check fails\n- `treatPendingAsSignedOut?` - Treat pending sessions as signed out (default: false)\n\n## Migrating from Protect\n\n`<Show>` replaces `<Protect>`, `<SignedIn>`, and `<SignedOut>`:\n\n```typescript\n// Before (deprecated)\n<Protect role=\"org:admin\">...</Protect>\n<Protect permission=\"org:billing:manage\">...</Protect>\n<Protect condition={(has) => has({ role: 'org:admin' })}>...</Protect>\n<SignedIn>...</SignedIn>\n<SignedOut>...</SignedOut>\n\n// After\n<Show when={{ role: 'org:admin' }}>...</Show>\n<Show when={{ permission: 'org:billing:manage' }}>...</Show>\n<Show when={(has) => has({ role: 'org:admin' })}>...</Show>\n<Show when=\"signed-in\">...</Show>\n<Show when=\"signed-out\">...</Show>\n```\n\nUse the `@clerk/upgrade` CLI to automatically migrate:\n\n```bash\nnpx @clerk/upgrade\n```\n\n## Custom Permissions\n\nDefine custom permissions in Clerk Dashboard under **Organizations** → **Roles & Permissions**:\n\nExample permissions:\n\n- `org:billing:manage`\n- `org:members:invite`\n- `org:settings:edit`\n- `org:content:publish`\n\n```typescript\n<Show when={{ permission: 'org:content:publish' }}>\n  <button>Publish Article</button>\n</Show>\n```\n\n## Important Notes\n\n- Returns `null` while auth is loading\n- Always performs authentication checks (user must be signed in for role/permission checks)\n- Can be used in both client and server components (Next.js)\n- For sensitive data, verify authorization on the server side\n\n\n---\n\n\n# Server-Side Auth in Next.js App Router\n\nAccess auth in Server Components, API routes, and Server Actions.\n\n**Requirements**: Next.js 13+ with App Router and `clerkMiddleware()` configured\n\n## Server Components\n\n```typescript\n// app/dashboard/page.tsx\nimport { auth, currentUser } from '@clerk/nextjs/server';\n\nexport default async function DashboardPage() {\n  // Get basic auth info\n  const { userId, orgId, orgRole } = await auth();\n\n  if (!userId) {\n    return <div>Please sign in</div>;\n  }\n\n  // Get full user object when needed\n  const user = await currentUser();\n\n  return (\n    <div>\n      <h1>Welcome, {user?.firstName}!</h1>\n      {orgId && <p>Organization: {orgId}</p>}\n    </div>\n  );\n}\n```\n\n## API Routes\n\n```typescript\n// app/api/data/route.ts\nimport { auth } from '@clerk/nextjs/server';\n\nexport async function GET() {\n  const { userId, orgId } = await auth();\n\n  if (!userId) {\n    return Response.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n\n  // Fetch data scoped to user or organization\n  const data = await db.items.findMany({\n    where: orgId ? { organizationId: orgId } : { userId },\n  });\n\n  return Response.json(data);\n}\n```\n\n## Server Actions\n\n```typescript\n// app/actions.ts\n'use server';\nimport { auth } from '@clerk/nextjs/server';\n\nexport async function createItem(formData: FormData) {\n  const { userId, orgId } = await auth();\n\n  if (!userId) {\n    throw new Error('Unauthorized');\n  }\n\n  const name = formData.get('name') as string;\n\n  await db.items.create({\n    data: {\n      name,\n      userId,\n      organizationId: orgId,\n    },\n  });\n}\n```\n\n## Protect Routes in Middleware\n\n```typescript\n// middleware.ts\nimport { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server';\n\nconst isProtectedRoute = createRouteMatcher(['/dashboard(.*)', '/api/private(.*)']);\n\nexport default clerkMiddleware(async (auth, req) => {\n  if (isProtectedRoute(req)) {\n    await auth.protect();\n  }\n});\n\nexport const config = {\n  matcher: ['/((?!.*\\\\..*|_next).*)', '/', '/(api|trpc)(.*)'],\n};\n```\n\n## Get Session Token for External APIs\n\n```typescript\nimport { auth } from '@clerk/nextjs/server';\n\nexport async function GET() {\n  const { getToken } = await auth();\n\n  // Get token for your backend\n  const token = await getToken();\n\n  // Get token for specific integration\n  const supabaseToken = await getToken({ template: 'supabase' });\n\n  // Use token to call external API\n  const response = await fetch('https://api.example.com/data', {\n    headers: { Authorization: `Bearer ${token}` },\n  });\n\n  return Response.json(await response.json());\n}\n```\n\n\n---\n\n\n# Clerk Backend SDK\n\nPerform admin operations like user management, organization management, and user invitations from your server.\n\n## Usage in Next.js App Router\n\n```typescript\nimport { clerkClient } from '@clerk/nextjs/server';\n// Or: '@clerk/remix/server', '@clerk/backend', etc.\n\nconst client = await clerkClient();\n\n// Get user by ID\nconst user = await client.users.getUser('user_xxx');\n\n// List users\nconst users = await client.users.getUserList({\n  limit: 10,\n  offset: 0,\n  orderBy: '-created_at',\n});\n\n// Update user metadata\nawait client.users.updateUser('user_xxx', {\n  publicMetadata: { role: 'admin' },\n  privateMetadata: { internalId: '12345' },\n});\n\n// Delete user\nawait client.users.deleteUser('user_xxx');\n```\n\n## Organization Management\n\n```typescript\nimport { clerkClient } from '@clerk/nextjs/server';\n\nconst client = await clerkClient();\n\n// Create organization\nconst org = await client.organizations.createOrganization({\n  name: 'Acme Inc',\n  createdBy: 'user_xxx',\n  publicMetadata: { plan: 'enterprise' },\n});\n\n// Add member to organization\nawait client.organizations.createOrganizationMembership({\n  organizationId: 'org_xxx',\n  userId: 'user_xxx',\n  role: 'admin',\n});\n\n// Update organization metadata (for billing, etc.)\nawait client.organizations.updateOrganization('org_xxx', {\n  publicMetadata: {\n    plan: 'pro',\n    stripeCustomerId: 'cus_xxx',\n    stripeSubscriptionId: 'sub_xxx',\n  },\n});\n\n// Remove member\nawait client.organizations.deleteOrganizationMembership({\n  organizationId: 'org_xxx',\n  userId: 'user_xxx',\n});\n```\n\n## Invitations\n\n```typescript\nimport { clerkClient } from '@clerk/nextjs/server';\n\nconst client = await clerkClient();\n\n// Create invitation\nawait client.invitations.createInvitation({\n  emailAddress: 'user@example.com',\n  redirectUrl: 'https://app.example.com/accept-invite',\n  publicMetadata: { referredBy: 'user_xxx' },\n});\n\n// Revoke invitation\nawait client.invitations.revokeInvitation('inv_xxx');\n```\n\n## Webhooks for User Events\n\n```typescript\n// app/api/webhooks/clerk/route.ts\nimport { Webhook } from 'svix';\nimport { headers } from 'next/headers';\nimport { WebhookEvent } from '@clerk/nextjs/server';\n\nexport async function POST(req: Request) {\n  const WEBHOOK_SECRET = process.env.CLERK_WEBHOOK_SECRET!;\n  const headerPayload = await headers();\n  const svix_id = headerPayload.get('svix-id');\n  const svix_timestamp = headerPayload.get('svix-timestamp');\n  const svix_signature = headerPayload.get('svix-signature');\n\n  const payload = await req.json();\n  const body = JSON.stringify(payload);\n\n  const wh = new Webhook(WEBHOOK_SECRET);\n  let evt: WebhookEvent;\n\n  try {\n    evt = wh.verify(body, {\n      'svix-id': svix_id!,\n      'svix-timestamp': svix_timestamp!,\n      'svix-signature': svix_signature!,\n    }) as WebhookEvent;\n  } catch (err) {\n    return Response.json({ error: 'Invalid webhook' }, { status: 400 });\n  }\n\n  switch (evt.type) {\n    case 'user.created':\n      // Sync to database, send welcome email, etc.\n      await db.users.create({\n        data: {\n          clerkId: evt.data.id,\n          email: evt.data.email_addresses[0]?.email_address,\n        },\n      });\n      break;\n\n    case 'organization.created':\n      // Create org in your database\n      await db.organizations.create({\n        data: {\n          clerkId: evt.data.id,\n          name: evt.data.name,\n        },\n      });\n      break;\n  }\n\n  return Response.json({ received: true });\n}\n```\n"}]},"jsonrpc":"2.0","id":1}

